---
- name: Example playbook
  hosts: localhost
  vars:
    my_var: "{{ lookup('ini', 'my_var section=defaults file=' + ansible_config) }}"
  tasks:
    - name: Print variable value
      debug:
        var: my_var
